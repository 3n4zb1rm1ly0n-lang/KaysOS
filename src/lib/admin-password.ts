/**
 * Veritabanı sıfırlama onayı için varsayılan.
 * Panel girişi Supabase Auth (e-posta + şifre) ile yapılır.
 * Özel onay şifresi: ADMIN_PASSWORD (.env)
 */
export const DEFAULT_ADMIN_PASSWORD = '141592';
